import parser from "./parser";
import instructions from "./instructions";
import retrieveTypes from "./retrieveTypes";
import validateJson from "./validateJson";


export = {
    parser,
    instructions,
    retrieveTypes,
    validateJson
};